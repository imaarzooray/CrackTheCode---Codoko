#include<stdio.h>

// entry of the program
int main()
{
   

 


    /*
    single line comment
    asdfasdf

    asdf
    */

    /*
    ?Comments:
    statement which is not executed by Compiler
    Two type: single line, multi line

    ? CONTROL STATEMENTS
    - which controls the flow of the program
    - Linear  - top down

   - If 
- if-else
    if-else-if
    if-else-ladder

    switch case 

    ? JUMP STATEMENT: 
    continue - loops/ conditons/ skips the single iteration/ one time
    break - skips the whole condition/ loop 


    go to - kahi pe jana



    ? KEYWORD: predefined word or built in word
    ? ex: if, int, float, main, auto, break, continue, switch

    ? IF
    Syntax: 

    ? {} - block
    if(condition)
        {
            ? code to be executed
        }

    ? IF-ELSE STATEMENT

    if(condition)
    {
      code to be executed
    }

    else
    {
        code to be executed
    }

    ? If-else-if

    ? 


    */

    printf("Enter your age:");

    int age;
    scanf("%d",&age);

    if(age>18)
    {
        printf("You can drive!");
    }
    else{
        printf("Go and Drive Bicycle!");
    }

    return 0;
}